# SQL-Data-Cleaning
Cleaned layoffs data by removing duplicates via window functions, standardizing text fields, handling NULLs, and enforcing schema integrity with staging tables for accurate downstream analysis.
